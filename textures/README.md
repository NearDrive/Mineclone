# Textures

Place block textures in this folder. The engine looks for `textures/dirt.png` at runtime, and all block textures are authored at **32x32** pixels.

If the dirt texture is missing, the engine will fall back to a procedurally generated 32x32 dirt texture.
